#include "VisionDisplay/GraphicObject.h"

namespace VisionDisplay {

// GraphicObject is currently a value type. This translation unit keeps the
// public class available as a stable DLL symbol as the overlay API grows.

} // namespace VisionDisplay
